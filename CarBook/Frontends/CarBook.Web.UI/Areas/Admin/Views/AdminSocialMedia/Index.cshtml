@model List<ResultSocialMediaDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Car SocialMedia Lists</h4>
                        <p class="card-subtitle mb-4">
                            Adding new SocialMedias
                        </p>
                        <div class="table-responsive">
                            <table class="table mb-0">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Name</th>
                                        <th>Icon</th>
                                        <th>Url</th>
                                        <th>Delete</th>
                                        <th>Create</th>
                                        <th>Update</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        count++;

                                        <tr>
                                            <th scope="row">@count</th>
                                            <td>@item.Name</td>
                                            <td>@item.Icon</td>
                                            <td>@item.Url</td>
                                            <td><a asp-area="Admin" asp-action="RemoveSocialMedia" asp-controller="AdminSocialMedia" asp-route-id="@item.SocialMediaID" class="btn btn-outline-danger">Remove</a></td>
                                            <td><a asp-area="Admin" asp-action="UpdateSocialMedia" asp-controller="AdminSocialMedia" asp-route-id="@item.SocialMediaID" class="btn btn-outline-success">Update</a></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                            <td><a asp-action="CreateSocialMedia" asp-controller="AdminSocialMedia" class="btn btn-outline-primary">Create New SocialMedia</a></td>

                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

